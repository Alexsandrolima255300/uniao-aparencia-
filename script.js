const form=document.getElementById("quoteForm");const result=document.getElementById("result");form.addEventListener("submit",e=>{e.preventDefault();const d=document.getElementById("destination").value;const w=Number(document.getElementById("weight").value||0);if(!d||w<=0){result.textContent="Informe destino e peso para simular.";return}const v=Math.max(100,55+w*.72);result.textContent="Estimativa visual: R$ "+v.toLocaleString("pt-BR",{minimumFractionDigits:2,maximumFractionDigits:2})});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const t=document.querySelector(a.getAttribute("href"));if(t){e.preventDefault();t.scrollIntoView({behavior:"smooth"})}}));