const form=document.getElementById("quoteForm");const result=document.getElementById("result");
form.addEventListener("submit",e=>{e.preventDefault();const d=document.getElementById("destination").value;const w=Number(document.getElementById("weight").value||0);const nf=Number(document.getElementById("invoice").value||0);const volumes=Number(document.getElementById("volumes").value||1);if(!d||w<=0){result.innerHTML='<span>●</span> Informe destino e peso para simular.';return}const v=Math.max(100,55+w*.72+(nf*.0015)+Math.max(0,volumes-1)*8);result.innerHTML='<span>✓</span> Estimativa visual: <strong>R$ '+v.toLocaleString("pt-BR",{minimumFractionDigits:2,maximumFractionDigits:2})+"</strong>"});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const t=document.querySelector(a.getAttribute("href"));if(t){e.preventDefault();t.scrollIntoView({behavior:"smooth"})}}));